<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" asp-page="Index">
        <img src="/assets/img/icon.svg" width="30" height="30" alt="icon"/>
        <span class="ml-4 text-accent">mesi.io</span>
    </a>
    <button
        class="navbar-toggler"
        type="button"
        data-toggle="collapse"
        data-target=".dual-collapse2">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div
        class="navbar-collapse collapse w-100 order-1 order-md-0 dual-collapse2">
        <div class="navbar-nav mr-auto">
            <a asp-page="Index" class="nav-item nav-link">Home</a>
            <a asp-page="Clipboard" class="nav-item nav-link">Clipboard</a>
        </div>
    </div>
    <div class="navbar-collapse collapse w-100 order-3 dual-collapse2">
        @if (!User.Identity.IsAuthenticated)
        {
            <div class="navbar-nav ml-auto">
                <a class="navbar-btn btn button-navbar nav-item nav-link navbar-login-button" asp-page="Login">Login</a>
            </div>
        }
        else
        {
            <div class="navbar-nav ml-auto" v-if="isLoggedIn">
                <span class="navbar-text user-welcome-text">
                    Hello @(User.Claims.FirstOrDefault(claim => claim.Type == "name")?.Value ?? "user")
                </span>
                <a class="navbar-btn btn button-navbar nav-item nav-link bg-dark text-white" asp-controller="Authentication" asp-action="Logout" >Logout</a>
            </div>
        }
    </div>
</nav>